{
  "version": 2,
  "framework": "nextjs",
  "installCommand": "pnpm install --no-frozen-lockfile",
  "buildCommand": "pnpm run build",
  "functions": {
    "api/**/*.{js,ts}": {
      "runtime": "nodejs20.x"
    },
    "app/api/**/*.{js,ts}": {
      "runtime": "nodejs20.x"
    }
  }
}
